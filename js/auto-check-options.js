jQuery(document).ready(function($) {
    // Marca automaticamente as opções quando a página é carregada
    $('#_downloadable').prop('checked', true).change();
    $('#_virtual').prop('checked', true).change();
});
